## A test Clojure build in Gradle with Java project dependencies

### Gradle
```
gradlew setup
```

### IntelliJ
 - import project with Gradle as external model
 - edit configurations -> new local clojure repl -> use clojure.main in normal JVM
 - build -> rebuild project
